apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: eloup-web
  namespace: eloup
  labels:
    app: eloup-web
spec:
  serviceName: eloup-web
  replicas: 1
  selector:
    matchLabels:
      app: eloup-web
  template:
    metadata:
      labels:
        app: eloup-web
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: eloup-web
          image: bholcombe/eloup-web:14347d73fb4ec3fb671c3f19ddf88b258d7f7256-dirty
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: eloup-web-config
            - secretRef:
                name: eloup-web-secret
          volumeMounts:
            - name: data
              mountPath: /data
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
      restartPolicy: Always
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: linode-block-storage-retain
        resources:
          requests:
            storage: 5Gi
