apiVersion: v1
kind: ConfigMap
metadata:
  name: eloup-web-config
  namespace: eloup
  labels:
    app: eloup-web
data:
  DISCORD_CLIENT_ID: "1504985018366169211"
  APP_DOMAIN: "https://eloup.kodloki.io"
  DATABASE_PATH: "/data/eloup.sqlite"
  AUTH_URL: "https://eloup.kodloki.io"
  AUTH_TRUST_HOST: "true"
  ELOUP_BOOTSTRAP_ADMIN_DISCORD_ID: "481702948146249728"
  # Temporary memory-leak diagnostic (diag-mem-probe.md). Exposes memory
  # counters on /api/health. Remove this line + roll to disable.
  DIAG_MEM: "1"
