# Archived task docs

Completed task documents, moved out of `Active/` for a cleaner working list.
Each is `## Status: Complete` with its outcome recorded inline; the matching
review report lives in `Agents/Review-reports/`. History preserved via `git mv`.

Umbrella/planning docs (`project-review-and-plan.md`, `ux-overhaul-plan.md`)
intentionally remain in `Active/` as In Progress until all their milestones land.
